import styled from 'styled-components/native'
import { COLORS } from 'app/utils/constants'

export const HeaderStyled = styled.Text`
  font-size: 24px;
  color: ${COLORS.black};
`

export const WikiStyled = styled.Text`
  font-size: 24px;
  color: ${COLORS.blueDemon};
`
